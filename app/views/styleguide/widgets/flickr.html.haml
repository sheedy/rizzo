= render 'styleguide/partials/shared'

.card.styleguide-intro.copy--body.context--content
  .clearfix
    %h1.split--left.copy--h1 Flickr widget

  %p
    This is the flickr widget

.grid-wrapper--10
  - flickr_widgets.each_with_index do |widget, i|
    = styleguide_component("widgets/flickr", properties: widget, num_items: 5, count: i, full_width: true)
