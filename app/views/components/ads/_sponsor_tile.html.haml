:ruby

  attrs = {
    id: "js-ad-sponsorTile-#{Time.now.to_i}",
    data: {
      size: { mapping: "sponsor-tile" },
      targeting: { position: nil },
      extension: nil
    }
  }

  attrs.merge!(properties) if properties

.adunit.adunit--sponsortile{ id: attrs[:id], data: attrs[:data] }
