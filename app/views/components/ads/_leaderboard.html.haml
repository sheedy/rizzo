:ruby

  attrs = {
    id: "js-ad-leaderboard-#{Time.now.to_i}",
    data: {
      size: { mapping: "leaderboard" },
      targeting: { position: nil },
      extension: nil
    }
  }

  attrs.merge!(properties) if properties

.adunit.adunit--leaderboard{ id: attrs[:id], data: attrs[:data] }
